# 🎬 과제보고서 스크린샷 - 터미널 명령 방식 (가볍게!)

## ✅ 최종 방법! (CPU 부담 없음)

GUI 1개만 실행하고, 터미널에서 간단한 명령으로 토스트 띄우기!

---

## 🚀 사용 방법

### 1단계: Jetson GUI만 실행

```bash
cd ~/jetson-food-ai/jetson1_monitoring
python3 JETSON1_INTEGRATED.py
```

> GUI 1개만 실행! CPU 부담 없음!

### 2단계: 새 터미널에서 이벤트 전송

```bash
cd ~/jetson-food-ai

# 대화형 메뉴 (번호 선택)
./send_toast_events.sh

# 또는 직접 번호 지정
./send_toast_events.sh 1  # 진동 측정 시작
./send_toast_events.sh 3  # POT1 짜장
./send_toast_events.sh 5  # POT2 볶음밥
```

### 3단계: 즉시 스크린샷!

토스트가 5초 동안 보임 → 여유롭게 캡쳐!

---

## 📋 이벤트 목록

```
1) 진동 측정 시작
2) 진동 측정 종료
3) POT1 짜장 투입
4) POT1 짬뽕 투입
5) POT2 볶음밥 투입
6) POT2 우동 투입
```

---

## 💡 더 간단한 방법 (직접 mosquitto_pub)

### 진동 측정 시작

```bash
mosquitto_pub -h 192.168.0.100 -t "HR/Status" -m '{
    "Status": [{
        "DeviceNum": "1",
        "ChkVibration": true
    }]
}'
```

### POT1 짜장 투입

```bash
mosquitto_pub -h 192.168.0.100 -t "HR/Status" -m '{
    "Status": [{
        "DeviceNum": "1",
        "PTNum": "0",
        "ProcessType": "투입",
        "NowRecipe": "짜장"
    }]
}'
```

### POT2 짬뽕 투입

```bash
mosquitto_pub -h 192.168.0.100 -t "HR/Status" -m '{
    "Status": [{
        "DeviceNum": "1",
        "PTNum": "1",
        "ProcessType": "투입",
        "NowRecipe": "짬뽕"
    }]
}'
```

---

## 📸 캡쳐 체크리스트

### Jetson #1
- [ ] 진동 측정 시작 토스트 (파란색)
- [ ] POT1 짜장 투입 토스트 (주황색)
- [ ] POT2 짬뽕 투입 토스트 (주황색)
- [ ] 야간 감지 화면 (개발자 모드)

---

## ⚡ 초간단 요약

```bash
# 터미널 1: GUI만 실행
python3 JETSON1_INTEGRATED.py

# 터미널 2: 명령 1줄로 토스트 띄우기
./send_toast_events.sh 1  # 진동 시작
./send_toast_events.sh 3  # POT1 짜장

# 📸 스크린샷!
```

**끝!**

---

## 💪 장점

✅ **CPU 부담 없음** - GUI 1개만 실행
✅ **간단함** - 터미널 명령 1줄
✅ **빠름** - 즉시 실행
✅ **안정적** - Segmentation fault 없음

---

## ⚠️ 문제 해결

### mosquitto_pub 명령이 없다면

```bash
sudo apt install mosquitto-clients
```

### MQTT 브로커 주소 확인

```bash
# config.json 확인
cat ~/jetson-food-ai/jetson1_monitoring/config.json | grep mqtt_broker
```

---

**만든 날짜:** 2026-02-03
**목적:** 과제보고서 GUI 스크린샷 (가볍게!)
**방법:** 터미널 명령으로 MQTT 메시지 전송

**화이팅! 🚀**
