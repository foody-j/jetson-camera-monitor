# Jetson2 작업 로그 - 2026-01-16

## 작업 요약
Jetson #2 튀김 AI 시스템 GUI 개선 및 데이터 수집 로직 강화

---

## 1. GUI 온도 라벨 → Color Diff 라벨 교체

### 문제
- GUI에 기름/탐침 온도가 표시되고 있었으나, 실제로는 color_diff가 더 중요한 지표
- color_diff는 카메라 프레임에만 `cv2.putText`로 표시되고 GUI 라벨에는 없었음

### 해결
**파일:** `jetson2_frying_ai/JETSON2_INTEGRATED.py`

**변경사항:**
- 기름/탐침 온도 라벨 제거
- `frying_left_color_diff_label`, `frying_right_color_diff_label` 추가

**코드 위치:**
- Line 1363-1366: POT1 color_diff 라벨 생성
- Line 1402-1405: POT2 color_diff 라벨 생성
- Line 1875: POT1 color_diff GUI 업데이트
- Line 2008: POT2 color_diff GUI 업데이트

**표시 형식:**
```
색상변화: 12.5
색상변화: 0.0  (baseline_not_set)
색상변화: --   (error)
```

**커밋:** `31ccf45` - Replace temperature labels with color_diff in GUI

---

## 2. 청소 모드 데이터 수집 필터링

### 문제
- MQTT로 `recipe: "0청소"` 같은 청소 메시지가 오면 데이터 수집이 시작됨
- 청소 중에는 데이터가 필요 없음

### 해결
**파일:** `jetson2_frying_ai/JETSON2_INTEGRATED.py`

**변경사항:**
- Line 1075: `is_cleaning = "청소" in recipe if recipe else False` 체크 로직 추가
- Line 1078, 1103: 투입/조리 조건에 `and not is_cleaning` 추가
- Line 1093-1094, 1119-1120: 청소 모드 감지 로그 출력

**로직:**
```python
# "청소" 키워드 필터링: recipe에 "청소"가 포함되면 수집 안 함
is_cleaning = "청소" in recipe if recipe else False

if process_type in ["투입", "조리"] and not is_cleaning:
    # 데이터 수집 시작
    ...
elif is_cleaning:
    print(f"[로봇상태] POT1(왼쪽) 청소 모드 감지 - 데이터 수집 스킵")
```

**로그 예시:**
```
[로봇상태] 튀김솥 PT0 | 조리 | 0청소 | 온도:34.0°C | 30분 51초
[로봇상태] POT1(왼쪽) 청소 모드 감지 - 데이터 수집 스킵
```

**커밋:** `7e21581` - Skip data collection when recipe contains '청소' keyword

---

## 3. GST Preview Window 제거

### 문제
- 카메라 시작 시 별도의 GST 윈도우가 팝업으로 뜸
- GUI에 카메라 프레임이 표시되지 않음
- `autovideosink`가 GStreamer 파이프라인에서 별도 윈도우를 생성

### 원인
**파일:** `jetson2_frying_ai/gst_camera.py`

**GStreamer 파이프라인 구조:**
```
v4l2src (카메라)
  → tee (분기)
      ├─ Branch 1: autovideosink (GST 윈도우) ← 문제!
      └─ Branch 2: fdsink (Python → tkinter GUI)
```

### 해결
**변경사항:**
- Line 26: `self.preview_sink = os.getenv("GMSL_PREVIEW_SINK", "autovideosink")`
  → `self.preview_sink = os.getenv("GMSL_PREVIEW_SINK", "none")`

**차이점:**
| Before | After |
|--------|-------|
| `autovideosink` | `none` |
| GST 윈도우 + Python 프레임 (2개 출력) | Python 프레임만 (1개 출력) |
| 별도 윈도우 팝업 | tkinter GUI에만 표시 |
| 디버그용 | 운영용 |

**GStreamer 명령어 변화:**
```bash
# Before
gst-launch-1.0 v4l2src device=/dev/video0 ! tee name=t \
  t. ! queue ! videoconvert ! autovideosink sync=false \
  t. ! queue ! videoconvert ! fdsink fd=1

# After
gst-launch-1.0 v4l2src device=/dev/video0 ! tee name=t \
  t. ! queue ! videoconvert ! fdsink fd=1
```

**커밋:** `583fbc3` - Disable GST preview window by default

---

## 기존 작업 상태 (이전 세션에서 완료)

### 4. "조리" 상태에서도 데이터 수집 시작
**상태:** ✅ 완료 (이전 커밋: `c6ee0cc`)

**로직:**
```python
if process_type in ["투입", "조리"]:  # "투입"만 → "투입", "조리" 둘 다
    if not self.pot1_collecting:
        self.start_pot1_collection()
```

**안전장치:**
- 중복 실행 방지: `if not self.pot1_collecting`
- 배출 타이머 취소: 재투입 시나리오 대응
- 충돌 없음 확인 완료

### 5. MQTT Callback 스레드 에러 수정
**상태:** ✅ 완료 (이전 커밋: `c6ee0cc`)

**문제:** "main thread is not in main loop" 에러

**해결:**
```python
# Before (에러 발생)
self.root.after(0, lambda: self.start_frying_camera("0"))
self.root.after(0, self.start_pot1_collection)

# After (정상 동작)
self.start_frying_camera("0")
self.start_pot1_collection()
```

### 6. RBMotion 기반 동적 수집 속도
**상태:** ✅ 완료 (이전 커밋)

**로직:**
- RBMotion 1 (POT1 움직임) → 0.5초 간격
- RBMotion 2 (POT2 움직임) → 0.5초 간격
- RBMotion 0/null (정지) → 1초 간격

### 7. DISCHARGE 상태 감지
**상태:** ✅ 완료 (이전 커밋)

**조건:**
```python
if self._compare_time(running_time, target_time) and color_diff >= 25.0:
    self.pot1_pot_status = "DISCHARGE"
```

**상태 전이:** IDLE → COOKING → DISCHARGE → IDLE

---

## 전체 커밋 히스토리

```bash
583fbc3 - Disable GST preview window by default (2026-01-16)
7e21581 - Skip data collection when recipe contains '청소' keyword (2026-01-16)
31ccf45 - Replace temperature labels with color_diff in GUI (2026-01-16)
c6ee0cc - Fix main thread error in MQTT callback (이전)
```

---

## 테스트 체크리스트

### GUI
- [x] color_diff 라벨이 POT1, POT2에 표시됨
- [x] GST 윈도우가 안 뜸
- [x] tkinter GUI에 카메라 프레임 정상 표시
- [ ] 실제 Jetson2에서 GUI 확인 필요

### 데이터 수집
- [x] "투입" 시 수집 시작
- [x] "조리" 시 수집 시작 (프로그램 런타임 중에도)
- [x] "청소" 키워드 포함 시 수집 안 함
- [x] 중복 시작 방지 동작
- [ ] 실제 Jetson2에서 데이터 수집 확인 필요

### MQTT
- [x] MQTT callback 스레드 에러 없음
- [x] RBMotion 기반 속도 조절 동작
- [x] DISCHARGE 상태 감지

---

## 배포 방법

```bash
# Jetson2에서 실행
cd ~/jetson-food-ai
git pull
sudo systemctl restart jetson2-monitor

# 로그 확인
sudo journalctl -u jetson2-monitor -f
```

**확인할 로그:**
```
[GstCamera] Creating camera for /dev/video0 @ 1920x1536
# "autovideosink"가 없어야 함 (preview_sink = "none")

[로봇상태] 튀김솥 PT0 | 조리 | 0청소 | 온도:34.0°C | 30분 51초
[로봇상태] POT1(왼쪽) 청소 모드 감지 - 데이터 수집 스킵

[로봇상태] 튀김솥 PT0 | 조리 | chicken | 온도:170.0°C | 05분 30초
[로봇상태] POT1(왼쪽) 데이터 수집 시작 (조리) - chicken
```

---

## 주요 파일

| 파일 | 역할 | 수정 내용 |
|------|------|----------|
| `jetson2_frying_ai/JETSON2_INTEGRATED.py` | 메인 프로그램 | GUI 라벨 교체, 청소 필터링 |
| `jetson2_frying_ai/gst_camera.py` | GMSL 카메라 핸들러 | GST preview 비활성화 |
| `jetson2_frying_ai/config_jetson2.json` | 설정 파일 | (변경 없음) |

---

## 다음 작업 (Optional)

### MQTT 상태 발행 기능 추가
현재 config에 `mqtt_topic_frying: "frying/status"`가 있지만 사용하지 않음.

**제안:**
```json
// Topic: frying/status (1초마다 발행)
{
  "pot1": {
    "status": "COOKING",  // IDLE, COOKING, DISCHARGE
    "color_diff": 12.5,
    "food_type": "chicken",
    "collecting": true
  },
  "pot2": {
    "status": "IDLE",
    "color_diff": 0.0,
    "food_type": null,
    "collecting": false
  }
}
```

로봇 PC에서 실시간 상태 모니터링 가능.

---

## 문제 해결 노트

### Q: 투입/조리 둘 다 수집하는데 충돌 안 나나?
A: 안 남. `if not self.pot1_collecting` 체크로 중복 방지됨.

### Q: GST 윈도우는 왜 뜨는 거였나?
A: `autovideosink`가 GStreamer tee 파이프라인에서 별도 출력을 만들어서.

### Q: 청소 필터링은 어떻게 동작?
A: `is_cleaning = "청소" in recipe` 체크 후, `and not is_cleaning` 조건 추가.

---

## 코드 스니펫

### Color Diff GUI 업데이트
```python
# Line 1875 (POT1)
self.frying_left_color_diff_label.config(text=f"색상변화: {color_diff:.1f}")

# Line 2008 (POT2)
self.frying_right_color_diff_label.config(text=f"색상변화: {color_diff:.1f}")
```

### 청소 모드 필터링
```python
# Line 1075
is_cleaning = "청소" in recipe if recipe else False

# Line 1078
if process_type in ["투입", "조리"] and not is_cleaning:
    ...
elif is_cleaning:
    print(f"[로봇상태] POT1(왼쪽) 청소 모드 감지 - 데이터 수집 스킵")
```

### GST Preview 비활성화
```python
# Line 26
self.preview_sink = os.getenv("GMSL_PREVIEW_SINK", "none")
```

---

**작성일:** 2026-01-16
**작성자:** Claude
**프로젝트:** jetson-food-ai
**대상 디바이스:** Jetson #2 (Frying AI)
