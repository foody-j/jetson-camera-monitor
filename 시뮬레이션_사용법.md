# Jetson2 시뮬레이션 모드 사용법

## 개요
시뮬레이션 모드는 **실제 카메라 대신 디스크에 저장된 이미지(JPG)를 순차적으로 읽어**
기존 AI 파이프라인(추론, GUI, 데이터 저장)을 그대로 실행하는 기능입니다.

- 실제 카메라 입력 → `SimulatedCamera.read()`로 대체
- 이미지가 끝까지 소모되면 다시 처음부터 반복
- AI 로직(추론/GUI/저장)은 그대로 동작

## 폴더 구조 (자동수집 기준)
자동수집으로 저장된 세션 폴더 구조는 아래 형태입니다.

```
~/AI_Data/pot1/session_YYYYMMDD_HHMMSS/음식명/
  camera_0/*.jpg
  camera_2/*.jpg

~/AI_Data/pot2/session_YYYYMMDD_HHMMSS/음식명/
  camera_1/*.jpg
  camera_3/*.jpg
```

- pot1: `camera_0`(튀김L), `camera_2`(바켓L)
- pot2: `camera_1`(튀김R), `camera_3`(바켓R)

## 실행 방법
### 1) pot1/pot2 최상위 경로로 실행
(가장 최근 세션을 자동 탐색)

```
python3 JETSON2_INTEGRATED.py --simulate ~/AI_Data/pot1 --simulate-pot2 ~/AI_Data/pot2
```

### 2) 특정 세션 폴더로 실행
(원하는 세션을 직접 지정)

```
python3 JETSON2_INTEGRATED.py --simulate ~/AI_Data/pot1/session_20240201_120000/치킨 \
  --simulate-pot2 ~/AI_Data/pot2/session_20240201_120000/치킨
```

## 동작 방식
1. `--simulate` / `--simulate-pot2` 경로에서 `camera_*` 폴더 탐색
2. 각 카메라 폴더의 JPG 목록을 정렬 후 순차 재생
3. 이미지 끝까지 가면 첫 이미지부터 반복
4. 기존 추론/GUI/저장 로직 그대로 진행

## 주의사항
- 시뮬레이션은 **카메라 입력만 대체**합니다.
- 자동수집 시작/종료는 여전히 MQTT 이벤트에 의존합니다.
  - 브로커가 없으면 자동수집이 시작되지 않습니다.
- 필요 시 MQTT 재생(로그 기반)까지 확장 가능합니다.

## 문제 해결 팁
- 이미지가 안 나오면 `camera_0/1/2/3` 폴더 경로 확인
- `jpg` 파일이 없는 경우 시뮬레이션 카메라는 비활성화됨

