# 🎬 과제보고서 GUI 스크린샷 - 초간단 가이드

## 목표

**실제 동작은 상관없고, GUI 화면에 이벤트가 떠있는 스크린샷만 찍으면 됨!**

---

## ✅ 준비 완료

1. **토스트 시간 5초로 늘림** - 여유롭게 캡쳐 가능
2. **MQTT 테스트 스크립트** - 버튼 클릭으로 이벤트 발생

---

## 🚀 스크린샷 찍는 방법

### 1단계: Jetson GUI 실행

```bash
# 터미널 1
cd ~/jetson-food-ai/jetson1_monitoring
python3 JETSON1_INTEGRATED.py
```

### 2단계: MQTT 테스트 실행

```bash
# 터미널 2
cd ~/jetson-food-ai
python3 test_mqtt_real_events.py
```

### 3단계: 스크린샷 찍기

#### 진동 이상 토스트

1. 테스트 GUI에서 **"진동 측정 시작"** 클릭
2. Jetson GUI에 "진동 측정 시작" 토스트 표시됨
3. **5초 안에 스크린샷 찍기** (F12 또는 Print Screen)

> 💡 "진동 이상!" 메시지를 원하면:
> - 실제 진동센서 연결 필요
> - 또는 아래 "수동 토스트" 방법 사용

#### 볶음 투입 토스트

1. 테스트 GUI에서 **"POT1 짜장"** 버튼 클릭
2. Jetson GUI에 "볶음 POT1: 짜장" 토스트 표시됨
3. **5초 안에 스크린샷!**

#### 야간 감지 화면

1. Jetson GUI에서 **"개발자 모드"** 버튼 클릭
2. 아래로 스크롤
3. "야간 감지: N장" 표시 확인
4. **스크린샷!**

> 💡 야간 스냅샷 카운트를 올리려면:
> - 개발자 모드 → "스냅샷 모드 즉시 시작" 버튼
> - 카메라 앞에서 움직이기

---

## 🎯 더 간단한 방법: 수동 토스트

원하는 메시지를 직접 띄우고 싶다면:

### Python 콘솔에서 직접 실행

```bash
# Jetson GUI가 실행된 상태에서
# 새 터미널 열고

python3 << 'EOF'
import paho.mqtt.client as mqtt
import json

client = mqtt.Client()
client.connect("192.168.0.100", 1883)

# 진동 이상 메시지 전송
payload = {
    "Status": [{
        "DeviceNum": "1",
        "ChkVibration": True
    }]
}
client.publish("HR/Status", json.dumps(payload))

# 5초 기다렸다가
import time
time.sleep(5)

# 볶음 투입 메시지
payload2 = {
    "Status": [{
        "DeviceNum": "1",
        "PTNum": "0",
        "ProcessType": "투입",
        "NowRecipe": "짜장"
    }]
}
client.publish("HR/Status", json.dumps(payload2))

client.disconnect()
EOF
```

---

## 📸 필요한 스크린샷 목록

### Jetson #1
- [ ] 진동 측정 시작 (파란 토스트)
- [ ] 볶음 POT1 투입 (주황 토스트)
- [ ] 볶음 POT2 투입 (주황 토스트)
- [ ] 야간 감지 UI (개발자 모드)

### Jetson #2
- [ ] 튀김 AI 분석 화면
- [ ] 바켓 감지 화면

---

## 💡 팁

### 토스트를 더 길게 띄우고 싶다면

```bash
# JETSON1_INTEGRATED.py 또는 JETSON2_INTEGRATED.py
# 1762번째 줄 (Jetson1) 또는 2014번째 줄 (Jetson2)

def show_toast(self, message, duration_ms=10000):  # 10초
```

### 특정 메시지만 원한다면

테스트 스크립트 수정:

```python
# test_mqtt_real_events.py에서
# 원하는 메시지로 변경 가능
```

### 진동 결과 메시지 (NORMAL/ABNORMAL)

진동 결과는 실제 센서 측정 후 나오므로,
**진동 측정 시작/종료 토스트만** 스크린샷 찍으면 됩니다.

만약 "진동 정상" 또는 "진동 이상!" 토스트가 필요하면:
- config.json에서 `vibration_test_mode: false` 설정
- 실제 진동센서 연결
- 또는 코드에서 임시로 `show_toast("진동 이상! (150/100 mm/s)")` 호출

---

## ⚡ 초간단 요약

```bash
# 1. Jetson GUI 실행
python3 JETSON1_INTEGRATED.py

# 2. MQTT 테스트 실행
python3 test_mqtt_real_events.py

# 3. 버튼 클릭 → 토스트 뜸 → 5초 안에 캡쳐!
```

끝!

---

**만든 날짜:** 2026-02-03
**목적:** 과제보고서 GUI 스크린샷 (간단하게!)
**토스트 시간:** 5초 (여유롭게 캡쳐 가능)

**화이팅! 😊**
